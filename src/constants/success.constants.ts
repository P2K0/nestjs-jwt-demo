import { SuccessMessage, SuccessMessageKeys } from './interface/constants.interface';

export const SUCCESS_MESSAGE: SuccessMessage = {
  DEFAULT_SUCCESS: '请求成功'
};

export const SUCCESS_CODE: Record<SuccessMessageKeys, number> = {
  DEFAULT_SUCCESS: 200
};
